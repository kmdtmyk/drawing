<% unless page.out_of_range? %>
  <div>
    <% offset = (page.current_page - 1) * page.limit_value %>
    <span><%= format_number page.total_count %>件中</span>
    <span><%= format_number offset + 1 %>〜<%= format_number offset + page.count%>件を表示</span>
  </div>
<% end %>
