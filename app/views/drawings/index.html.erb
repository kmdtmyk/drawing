<% content_for :sidebar do %>
  <div class='px-3'>
    <h2>検索条件</h2>
    <%= render 'search_form' %>
  </div>
<% end %>

<div class='px-4'>

  <div class='d-flex justify-content-start align-items-center'>
    <h1 class='mr-4'>図面一覧</h1>
    <%= link_to '新規作成', new_drawing_path, class: 'btn btn-info' %>
  </div>

  <%= paginate_statistic @drawings %>

  <%= render 'search_conditions' %>

  <div class='card-columns'>
    <% @drawings.each do |drawing| %>
      <div class='card' >
        <%= link_to drawing do %>
          <img class='card-img-top' src='https://placehold.jp/400x300.png'>
        <% end %>
        <div class='card-text px-2 py-1'>
          <table>
            <tr>
              <th>受注先</th>
              <td><%= drawing.customer_name %></td>
            </tr>
            <tr>
              <th>品名</th>
              <td><%= drawing.product_name %></td>
            </tr>
            <tr>
              <th>品番</th>
              <td><%= drawing.part_number %></td>
            </tr>
            <tr>
              <th>見積もり日</th>
              <td><%= drawing.estimate_date %></td>
            </tr>
            <!-- <tr>
              <th>見積金額</th>
              <td><%= format_number drawing.estimated_price %></td>
            </tr> -->
          </table>
        </div>
      </div>
    <% end %>
  </div>

  <%= paginate @drawings %>

</div>
